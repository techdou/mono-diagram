{
  "name": "mono-diagram-skill",
  "version": "1.2.0",
  "private": true,
  "description": "Monochrome academic diagram rendering helpers for Mermaid and SVG.",
  "license": "MIT",
  "scripts": {
    "validate": "python3 scripts/mono_diagram.py validate assets/templates --recursive",
    "render": "python3 scripts/mono_diagram.py render assets/templates -o gallery --recursive",
    "test": "python3 -m unittest discover -s tests -p 'test_*.py'"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "10.9.1",
    "sharp-cli": "4.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
